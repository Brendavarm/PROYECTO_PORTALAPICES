# /api usa el proxy de Vite (recomendado para probar desde el celular en la misma WiFi)
VITE_API_URL=/api
# Opcional: IP de tu PC en la red local, ej. http://192.168.1.45:5173
# Si está vacío, el QR usa la URL del navegador (abre el sitio desde tu IP, no desde localhost)
VITE_APP_URL=
VITE_SOCIAL_URL=https://www.instagram.com/unifranzoficial
